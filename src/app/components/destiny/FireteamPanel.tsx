'use client'

import FlierTeamFinderPanel from '@/app/components/destiny/FlierTeamFinderPanel'

/** Fireteams tab — FlierTeam Finder LFG rooms. */
export default function FireteamPanel({ darkMode }: { darkMode: boolean }) {
  return <FlierTeamFinderPanel darkMode={darkMode} />
}
